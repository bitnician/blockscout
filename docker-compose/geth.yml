version: "3.9"

services:
  redis-db:
    extends:
      file: ./services/redis.yml
      service: redis-db
    networks:
      - blockscout-network

  db-init:
    extends:
      file: ./services/db.yml
      service: db-init
    networks:
      - blockscout-network

  db:
    depends_on:
      db-init:
        condition: service_completed_successfully
    extends:
      file: ./services/db.yml
      service: db
    networks:
      - blockscout-network

  backend:
    depends_on:
      - db
      - redis-db
    extends:
      file: ./services/backend.yml
      service: backend
    networks:
      - blockscout-network
    links:
      - db:database
    environment:
      ETHEREUM_JSONRPC_VARIANT: "geth"
      ETHEREUM_JSONRPC_URL: "http://igra-dev-el:8545"
      ETHEREUM_JSONRPC_WS_URL: "ws://igra-dev-el:8545"
      ETHEREUM_JSONRPC_HTTP_URL: "http://igra-dev-el:8545"
      ETHEREUM_JSONRPC_TRACE_URL: "http://igra-dev-el:8545"
      CHAIN_ID: "2600"

  visualizer:
    extends:
      file: ./services/visualizer.yml
      service: visualizer
    networks:
      - blockscout-network

  sig-provider:
    extends:
      file: ./services/sig-provider.yml
      service: sig-provider
    networks:
      - blockscout-network

  frontend:
    depends_on:
      - backend
    extends:
      file: ./services/frontend.yml
      service: frontend
    networks:
      - blockscout-network
    environment:
      NEXT_PUBLIC_NETWORK_ID: "2600"
      NEXT_PUBLIC_NETWORK_RPC_URL: "http://igra-dev-el:8545"
      HOST: "0.0.0.0"

  stats-db-init:
    extends:
      file: ./services/stats.yml
      service: stats-db-init
    networks:
      - blockscout-network

  stats-db:
    depends_on:
      stats-db-init:
        condition: service_completed_successfully
    extends:
      file: ./services/stats.yml
      service: stats-db
    networks:
      - blockscout-network

  stats:
    depends_on:
      - stats-db
      - backend
    extends:
      file: ./services/stats.yml
      service: stats
    networks:
      - blockscout-network

  user-ops-indexer:
    depends_on:
      - db
      - backend
    extends:
      file: ./services/user-ops-indexer.yml
      service: user-ops-indexer
    networks:
      - blockscout-network

  proxy:
    depends_on:
      - backend
      - frontend
      - stats
    extends:
      file: ./services/nginx.yml
      service: proxy
    networks:
      - blockscout-network

networks:
  blockscout-network:
    name: blockscout-network
    external: true
